# rounding decimal : there are 5 way of rounding
#  off the decimal in numpy : trancation,fix,rounding,floor,ceil

#truncation: trunc() and fix()

# here we are truncating the below array
import numpy as np
a = np.trunc([-3.1666,3.6667])
print(a)